---
os: linux
language: python
python: 3.7

branches:
  only: master

env:
  - TASK=fmt-travis
  - TASK=lint
  - TASK=test
  - TASK=coverage

jobs:
  include:
    # VERIFICATION OF TEST INFRASTRUCTURE
    - name: "run yamllint on .travis.yml"
      python: "3.7.7"
      install: pip install yamllint
      script: make -f Makefile yamllint

script: make -f Makefile $TASK
